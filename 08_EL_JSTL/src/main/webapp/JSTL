# JSTL(JSP Standard Tag Library)
- JSP 페이지에서 스크립트릿(자바코드)과 HTML 코드가
  뒤섞이게 되는 현상이 발생함.
  
- 이렇게 뒤섞인 코드는 알아보기 쉽지않은 단점이 있음.
  그래서 가독성 낮음.
  
- JSP 페이지에서 스크립트릿에 들어가는 자바코드 중에 논리적인 판단,
  반복 처리, 포맷 처리, 등을 HTML 태그처럼 쓸 수 있게 만들어서 정의한
  게 JSTL 라이브러리임.
 
- JSTL을 쓰려면 라이브러리 파일이 있어야됨.
  *jstl.jar
  *standard.jar
  
  
  